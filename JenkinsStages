stage ('Build the module') {
  // Just execute the yarn commands.
  sh '''
    yarn install
    yarn build
  '''
}

stage ('Publish to NPM') {
  dir ('app-resource-sharing') {
    sh '''
      npm publish
    '''
  }
}